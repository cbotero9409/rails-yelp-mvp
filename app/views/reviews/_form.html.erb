<%= simple_form_for [restaurant, review], url: restaurant_create_review_path(restaurant) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Save review", class: "btn btn-primary" %>
<% end %>
